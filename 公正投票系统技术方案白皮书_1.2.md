

**副标题**：基于双密码机制 + 可验证真随机数 + 去信任化结算的抗胁迫匿名投票系统
**版本**：v2.1（融合双密码机制、可验证真随机数与去信任化结算层）
**性质**：技术方案说明（供评审 / 立项 / 合规审查使用）
作者：ethanycq

---

## 摘要

本方案提出一套**抗胁迫（coercion-resistant）匿名投票系统**，由两大支柱构成：

1. **双密码机制**——每位投票者持有一个**真密码**与任意多个**防胁迫密码**。两类密码在位数、输入方式、页面反馈上**完全不可区分**，但系统记录的内容不同：真密码投出真实意愿，防胁迫密码投出弃权。
2. **可验证真随机数（VPR）**——系统所有随机性来源（随机代号、门限密钥分片、时间分桶偏移、审计链创世盐）均由**不可预测、事后可验证**的真随机数驱动，杜绝"内鬼反推身份映射"与"审计链事后伪造"。
3. **去信任化结算层**——通过同态加密（单票永不解密）、分布式密钥生成（完整私钥从未存在）、时间锁兜底（数学保证能出结果），把"信任机构不作恶"改造为"机构作恶代价极高、收益极低、且必定被发现"。

系统的安全基石是四条原则：

1. **真密码优先**：真实票一经投出即固化，任何后续胁迫票无法覆盖。
2. **不可区分性**：胁迫者即使全程监视投票过程，也无法判断投票者使用了哪类密码。
3. **可验证随机性**：所有随机数锚定公共熵源，第三方可独立重放验证，系统无法伪造。
4. **让胁迫无利可图**：胁迫者从被胁迫者身上只能得到"弃权"，而非其期望的选项。
5. **去信任化结算**：不信任任何单一机构，通过数学手段让"作恶"在收益上失去意义、在后果上无害、在行为上可被验证。

本方案适用于**组织内部决策投票**（工会选举、董事会/理事会表决、业委会、行业协会、学术评审等），用于防止管理层或权力方对个体投票者的干预。

---

## 1. 设计目标与威胁模型

### 1.1 设计目标

| 目标 | 说明 |
| --- | --- |
| 抗胁迫 | 投票者在被监视、被施压情境下，仍能确保最终票数不反映胁迫者意志 |
| 匿名性 | 任何人在任何阶段无法将某张选票关联到某个投票者 |
| 可验证性 | 结果与随机性可被第三方验证未被篡改，但不泄露个体选择 |
| 不可验收 | 投票者无法向他人证明自己投了什么，从而杜绝选票买卖 |
| 可用性 | 普通投票者无需密码学知识即可正确使用 |

### 1.2 攻击者能力边界（重要前提）

本方案**明确假设**攻击者（组织内权力方）具备以下能力：

- ✅ 物理监视投票者操作（站在身后、录屏、要求截图）
- ✅ 要求投票者当面投票、反复投票
- ✅ 口头施压、威胁、利诱
- ✅ 控制组织内部 IT 政策

同时**明确假设攻击者不具备**以下能力：

- ❌ 接触应用服务器、数据库、代码仓库
- ❌ 收买或胁迫托管机构中**达到门限数量**的成员
- ❌ 对投票者实施非法拘禁或暴力
- ❌ 预测或操控公共熵源（外部市场 / 区块链 / 彩票等）

> **本方案的全部安全性均建立在"服务器与数据库独立于攻击者"这一前提之上。** 若该前提被打破，需重新进行威胁建模。评审时应首先确认此前提能否由组织章程或第三方托管协议保障。

### 1.3 明确不防御的威胁

诚实声明：以下场景本方案**无法**防御，需通过制度与法律途径解决。

1. **被迫沉默**：胁迫者在投票期内全程隔离投票者，使其无法在任何安全时刻投出真实票。此时该票结果为弃权。
2. **选举被取消或结果不被承认**：胁迫者宣布投票无效、无视结果。
3. **自愿被收买**：投票者自愿使用真密码投出胁迫者期望的选项。系统不阻止，也不应阻止。
4. **暴力人身伤害**：胁迫者以暴力造成的人身伤害本身。技术无法阻止暴力，但暴力也**无法逼出真密码**——被逼问时给出任意防胁迫密码，对方同样无法验证真伪（见 5.3）。因此"逼问出真密码"在技术上是被覆盖的，唯一防不住的是暴力伤害本身，需法律救济。

> **说明：策略投票不属于"不防御的威胁"，而是应当被尊重。** 投票者基于自身偏好与判断投出非第一偏好选项（如为避免最厌恶者当选而转投次优者），是**自由意志的表达、本身就是民意**。本方案只防"被迫"，不防"自主"——系统不干涉、也不应干涉策略投票，也不将其视为操纵或异常。

---

## 2. 核心机制：双密码体系

### 2.1 双密码

| | 真密码 | 防胁迫密码 |
| --- | --- | --- |
| 使用场景 | 在可信环境、可信设备上投票 | 被胁迫、被监视、使用不可信设备时 |
| 系统记录 | 投票者的实际选择 | 弃权 |
| 页面反馈 | "您的选票已成功记录。" | **完全相同** |
| 数量 | 1 个 | **不限，可随时新增** |

**不可区分性要求**（硬性）：

- 所有密码的**位数完全一致**（防止胁迫者通过观察掩码黑点数量区分）
- 输入方式、错误提示、登录耗时、投票后跳转流程**完全一致**
- 页面、截图、录屏中**不出现任何**提示当前使用密码类型的元素

### 2.2 真密码优先原则

**真实票一经投出即永久固化，防胁迫票无法覆盖。**

```
若该投票者存在真密码投票记录 → 以【最后一次真密码投票】为准
否则若存在防胁迫投票记录     → 记为【弃权】
否则                          → 记为【未投票】
```

这条原则是整个方案的抗胁迫基石。它意味着：

- 胁迫者逼迫投票者"再投一次"，得到的仍是弃权，真实票不受影响
- 投票者无需守候截止时刻，**决胜点在于"何时安全地投出真实票"，而非"最后一次操作"**

### 2.3 能加，不能删

| 操作 | 权限 | 设计理由 |
| --- | --- | --- |
| 新增防胁迫密码 | 用户随时自助 | 胁迫者逼迫新增的密码仍是防胁迫密码，无害 |
| 删除/修改防胁迫密码 | 必须本人到指定第三方机构，全程录像 | 防止胁迫者逼迫投票者解除保护 |
| 修改真密码 | 必须本人到指定第三方机构 | 同等保护 |

**不对称设计是关键**：胁迫者可以逼你"加"，但加多少都无意义（都是弃权）；胁迫者无法逼你"删"，因为你自己也删不掉。

### 2.4 反馈不可区分

无论投票者处于何种状态，投票页面**永远且仅**显示：

> **"您的选票已成功记录。"**

页面**不显示**：历史投票记录、当前选票内容、剩余天数、是否已截止、修改次数。

因此，**截图与录屏不具备任何验收价值**——这是瓦解"选票买卖"的关键：买票者无法验证投票者是否履约，理性贿选者将放弃此类投资。

### 2.5 提前设置窗口

防胁迫密码的设置须在**投票系统上线前半年**完成。

理由：此时投票议题尚未公布，无人有动机监视设置过程。安全意识的最高形态，是在威胁尚不存在时完成防御部署。

**设置期要求**：

- 通过个人可信渠道（个人手机/个人邮箱，非公司渠道）引导完成
- 设置后立即进行一次模拟演练
- 演练结果**仅通过个人可信渠道反馈给本人**，不聚合、不公布、不留存

### 2.6 统一公开投票窗口

**取消随机截止时间，采用统一公开投票窗口**（如 30 天，所有人一致）。

理由：

- 胁迫者知晓截止日**无害**——他既无法覆盖真实票，也无法逼出真密码
- 随机截止会造成双向信息不对称，投票者可能在不知情中错过投票（自伤）
- 统一窗口简单透明，便于运营提醒与用户规划

**截止后行为**：设置 24 小时宽限期（宽限期内真实记录），宽限期后返回"成功"但不记录，防止胁迫者通过试探发现系统状态变化。

---

## 3. 核心机制：可验证真随机数（VPR）

### 3.1 为什么需要真随机数

原方案中的「随机代号」「时间分桶随机偏移」「门限密钥分片」「审计链盐值」都依赖随机数。若使用软件伪随机数（PRNG），存在两个致命隐患：

1. **可预测 → 匿名性归零**：服务器若被攻破或内鬼获知 PRNG 种子，可重放生成序列，反推"代号 → 用户"映射，匿名性瞬间崩溃。
2. **不可审计 → 可验证性打折**：伪随机数是系统自己"拍脑袋"生成的，无法向第三方证明"这串随机数没被操控过"，审计链的信任根基动摇。

因此引入**可验证公共真随机数（VPR, Verifiable Public Randomness）**，必须同时满足两个硬性要求：

- **不可预测**：投票期间，任何人（含系统管理员、攻击者）都无法预测随机数的值；
- **事后可验证**：随机数生成后，任何第三方都能独立重放计算，证明它确实来自声明的公共熵源、且未被篡改。

### 3.2 方法：承诺-揭示 + 多源公共熵组合

VPR 的核心思想是：**把随机性交给系统外部的、任何人都不可控的公共事件**。分四步完成：

```
选定熵源 → 事前承诺 → 到点揭示 → 组合验证
```

#### 步骤一：选定熵源（投票开始前公示）

熵源必须是「未来的、公共的、独立不可控的」事件。建议选取 **3–5 个独立熵源**，防止单一源被操控：

| 熵源示例         | 取值         | 不可控性来源     |
| ------------ | ---------- | ---------- |
| 12国本土公认市场基准  | 单位精确到 0.01 | 公开市场，多方博弈  |
| 12个国家首都当日降水量 | 单位精准到0.1   | 自然现象，完全不可控 |


国家	本土公认市场基准（最具影响力）
中国	沪深300
美国	S&P 500（标普500）
俄罗斯	MOEX Russia Index
英国	FTSE 100（富时100）
德国	DAX 40
韩国	KOSPI
沙特阿拉伯	TASI（Tadawul All Share）
法国	CAC 40
日本	Nikkei 225（日经225）
阿联酋	ADX General（阿布扎比）
以色列	TA-35
印度	BSE Sensex









#### 步骤二：事前承诺（Commit，投票开始前）

投票开始前，系统对每个熵源公示「事件标识 + 承诺哈希」：

```
Commit_i = SHA-256( event_desc_i  ‖  source_i  ‖  announced_at )
```

- `event_desc_i`：该熵源"将要发生的具体事件"的描述（如「比特币第 800000 号区块」）
- 承诺哈希一旦公示即锁定，不可更改

#### 步骤三：到点揭示（Reveal，事件发生后）

熵源事件在预定时间发生后，其值**公开可查**，任何人可独立获取原始数据（查询区块链浏览器、行情接口、官方开奖公告等）。

#### 步骤四：组合与验证（Combine & Verify）

将各熵源值按公示规则组合成真随机数种子：

```
R = SHA-256( entropy_1  ‖  entropy_2  ‖  …  ‖  entropy_k  ‖  domain_sep )
```

- `domain_sep`：用途分隔符（如 `"ballot-token"` / `"threshold-key"` / `"audit-chain"`），同一组熵源可派生多个用途的随机数而互不干扰
- **可验证**：任何第三方用公开的熵源值重放此哈希计算，即可验证 R 的真伪
- **不可伪造**：熵源是外部公共事件，系统无法篡改；承诺哈希在前，事后无法"补一个合意的值"

### 3.3 满足两大性质的论证

| 性质 | 如何满足 |
| --- | --- |
| **不可预测** | 熵源是"未来的公共事件"（下一区块、明日收盘价、下期彩票），投票期间其值尚未产生，任何人无法预测；多源组合进一步放大不可预测性 |
| **事后可验证** | 熵源值事后公开可查，组合算法是公开的哈希，任何人重放计算即可验证；承诺哈希锁定了"熵源清单"不可中途替换 |

### 3.4 在投票系统中的四个应用点

| 应用点 | 原方案（伪随机） | VPR 改造后 |
| --- | --- | --- |
| **随机代号生成** | `random()` + 盐 | 用 `R(ballot-token)` 派生的真随机数作代号与盐，杜绝"预测 PRNG 反推身份映射" |
| **门限密钥分片生成** | 密钥库生成 | 各托管方分片生成时混合 `R(threshold-key)`，保证分片不可预测 |
| **时间戳分桶偏移** | 固定偏移 | 用 `R(time-offset)` 做随机偏移，切断投票时间与身份的时间关联 |
| **审计链创世盐** | 系统自设盐值 | 创世盐 = `R(audit-chain)`，锚定公共熵源，第三方可验证整条链非"事后重算" |

### 3.5 时序安排

| 时点 | 动作 |
| --- | --- |
| T-7 天（投票前） | 公示熵源清单 + 各源承诺哈希 |
| T-1 天（投票前） | 熵源事件全部发生，揭示熵源值，生成真随机数种子 R，锁定系统随机性 |
| T-0 起（投票中） | 用 R 派生随机代号、时间偏移、审计盐；门限密钥分片生成 |
| 投票结束后 | 第三方用公开熵源重放验证 R → 进而验证审计链与随机性未被操控 |

### 3.6 可选增强

- **后置锚定**：审计链的**最后一个块**额外锚定"投票截止日之后的公共事件"（如截止次日的某指数收盘价），使整条链在投票期间**无法被提前伪造**，只能等待该事件发生后才能闭合。
- **门限联合生成**：若需更高强度，可由各托管方各自贡献熵分片 + 承诺揭示，最终 `R = XOR(各分片)`，任何两家联合即可生成、但无法单方预知——与门限密钥思想同构。

---

## 4. 系统架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────┐
│  认证层   独立密码认证（禁止 SSO 单点登录）      │
├─────────────────────────────────────────────┤
│  投票层   客户端加密 → 服务器只存密文            │
├─────────────────────────────────────────────┤
│  存储层   真随机代号 + 密文选票 + 哈希审计链      │
├─────────────────────────────────────────────┤
│  随机层   VPR 可验证真随机数（贯穿以上所有层）    │
├─────────────────────────────────────────────┤
│  结算层   同态求和 + 门限解密 + 时间锁兜底        │
└─────────────────────────────────────────────┘
```

> 随机层是新增的横向基础设施：认证、投票、存储、结算的每一个随机性环节，统一由 VPR 提供，避免任何"散落的伪随机数"成为匿名性破口。

### 4.2 认证层

**强制要求独立密码认证，禁止使用 SSO / 扫码 / 指纹 / 免密登录。**

现实中胁迫者往往同时控制组织 IT 政策。若本次投票被要求"统一走企业微信登录"，则双密码机制无处安放，整套方案失效。

因此认证方式必须由**外部契约锁定**（托管协议 / 选举章程），明确：

- 必须使用独立密码
- 每次提交选票需重新认证
- 会话 5 分钟超时
- **任何一方不得单方面变更认证方式**

这是一条**制度约束**，而非代码实现。

### 4.3 投票层：客户端加密

**关键设计：选票在客户端（浏览器 / App）本地加密后上传，服务器只存密文。**

```
密钥     = KDF(用户输入的密码)          # 真密码与防胁迫密码派生不同密钥
选票明文 = 实际选项（真密码） 或 弃权（防胁迫密码）
上传内容 = encrypt(选票明文, 密钥)
```

服务器**自身无法判断**某张票来自真密码还是防胁迫密码。这一设计一次性消除三类风险：

- 内鬼查库无法得知谁投了什么
- 数据库中不存在"我被胁迫过"的标记字段
- 服务器被攻破时，泄露的仅为密文

### 4.4 存储层

- 投票表**不存用户 ID**，只存由 VPR 派生的**真随机代号**
- 路由映射（用户 → 代号）在会话期间临时生成，**会话结束即销毁**，不落库
- 资格核验使用独立名单，仅记录"某人有资格"，不记录"某人对应哪个代号"
- 代号生成时拼接 VPR 派生盐值，防止反推

### 4.5 结算层：密钥托管的去信任化设计

**核心问题**：解密密钥由"三家机构"分片持有，这是整套方案里唯一残留的**人为信任环节**——一旦机构被收买、串通或胁迫，匿名性与结果公正性都可能崩塌。能否用数学手段替代这一人为环节？

**诚实结论：不能完全消除，但可以改造到几乎无害。** 私钥是秘密，秘密必然存在于某个物理载体上。密码学可以把秘密切成 N 份、要求 K 份才可恢复、保证完整形态从未出现过——但"谁持有这一片"这个问题，数学回答不了，它永远是一个人为安排。

因此去信任化的目标不是"消灭机构"，而是三点：**① 把信任要求从"相信他们不作恶"降为"他们作恶必定被发现"；② 把失效后果从"密钥泄露 = 全员暴露"降为"密钥泄露 = 只提前知道总数"；③ 把"何时解密"从人为约定改为数学决定。**

#### 4.5.1 五层数学加固

| 层次 | 技术 | 解决了什么 | 依然解决不了什么 |
| --- | --- | --- | --- |
| L1 分布式密钥生成 | Pedersen DKG / FROST | 完整私钥从未在任何时刻、任何设备上存在，消除"切割那一刻"的单点故障 | 不减少需信任的分片持有方数量 |
| L2 全同态加密 | Paillier / BFV / CKKS | 单张选票永不解密，只解密总和 | 密钥仍需人持有 |
| L3 零知识证明 + 公告板 | zk-SNARK / Bulletproofs | 任何人可独立验证计票正确性，作弊可检测、可追责 | 不阻止解密，只让作弊可检测 |
| L4 可验证延迟函数 | Wesolowski / Pietrzak VDF | 解密时刻由数学决定，而非人为约定 | 无法防止单方面解密 |
| L5 可追责门限 | Accountable threshold | 作弊后可数学指认是哪几片参与 | 事后追责，非事前阻止 |

#### 4.5.2 L2 全同态加密（性价比最高的一招）

以 Paillier 为例，其具备加法同态性：`E(m₁) × E(m₂) = E(m₁ + m₂)`。

因此计票**根本不需要解密任何一张票**——将所有选票密文在模运算下相乘，直接得到 `E(A 的总数)`、`E(B 的总数)`……只需解密这几个总数。**单张选票从加密到销毁，从未被解密过。**

| 密钥泄露的后果 | 传统门限（逐票解密） | 同态求和 |
| --- | --- | --- |
| 泄露范围 | 所有人的选择暴露，灾难性 | 只有最终总数提前泄露（而该数字本来就要公布） |

这一层不改变信任结构，却把人为环节失效的后果从**灾难降级为轻微**——机构即使全部串通，也只能提前知道一个本来就要公开的数字。

> **与双密码机制的衔接（关键）**：同态求和无法按用户合并多张票，但本方案的双密码机制天然适配——**防胁迫密码永远投"弃权"，弃权票在计票时被剔除（见 7.1），真密码投出的真实选项票被保留**。因此"真密码优先"在同态求和下自动成立：无论被胁迫投多少次弃权，弃权一律被剔除；只有真密码投的那张真实票有效。
>
> 代价：真密码默认**即投即固化、仅投一次**（与"真实票落地即固化"原则一致）；若需改票，由客户端在重投时生成同态"撤销票"抵消旧票（可选增强，非必需）。

#### 4.5.3 L1 分布式密钥生成（DKG）

传统门限方案先生成完整私钥、再切分——"切割的那一刻"完整密钥存在，是单点故障。**DKG 让 N 方各自生成随机片，通过交互协议共同产出公钥，私钥从未以完整形态在任何设备上出现过。**

分片生成须混合 VPR 真随机数（见 3.4），确保分片不可预测、不可复现。

#### 4.5.4 L4 可验证延迟函数（时间锁）——关键权衡

VDF 要求解密者连续执行 T 次无法并行加速的运算，使密钥"到点自动可解"。

但须明确一个**反直觉的权衡**——时间锁与人为门限防的是**相反**的东西：

| | 人为门限 | 时间锁 VDF |
| --- | --- | --- |
| 防"单方面偷偷解密" | ✅ 须串通才能解 | ❌ 时间一到谁都能解 |
| 防"机构集体缺席"（liveness） | ❌ 他们不来就卡死 | ✅ 数学保证一定能解 |

时间锁去掉了人为环节，却**同时去掉了"防止单方面行动"的能力**。因此它不能替代门限，只能与之组合、互补：

- **正常路径**：门限解密——快、可控，能防止单方面行动
- **兜底路径**：截止后 T 天，若门限方集体失联或被胁迫拒不到场，时间锁自动生效——防止拒绝服务

T 须设得远长于正常计票所需时间（如正常 1 天、T = 30 天），确保兜底永远不会抢在正常路径之前触发。

#### 4.5.5 L3 零知识证明 + 公开公告板 & L5 可追责门限

每张选票附一个 ZKP，证明"我加密的是一个合法选项（A/B/C/弃权之一）"，但不透露是哪一个；计票过程亦附 ZKP，证明"我正确地对所有密文求和，未做篡改"。任何人（候选人、外部审计）均可使用公开数据独立验证全过程。

作弊发生后，可追责门限能数学指认参与解密的分片集合，使追责成为可能。

#### 4.5.6 非密码学补充：让对立利益相关方各持一片

与其寻找"中立机构"，不如让**利益对立的各方各持一片**：工会一片、管理层一片、外部审计一片、员工代表一片……

任何一方想作弊，都须说服立场根本对立的另外数方同时配合。这依赖博弈论而非密码学，但在现实中往往更可靠——利益对立方之间天然缺乏串通动机。

建议门限规模由 2-of-3 提升至 **5-of-9**，并至少包含两个立场对立的利益相关方。

#### 4.5.7 推荐架构与信任模型

```
选票上传 FHE 加密 + ZKP 合法性证明 + 写入公开公告板
   ↓
计票阶段 同态求和（单票永不解密）→ 只得到【加密的总数】
   ↓
解密阶段 K-of-N 门限（DKG，完整密钥从未存在）→ 只解密总数
   ↓
兜底 截止 T 天后门限未完成 → VDF 时间锁自动可解
   ↓
验证 任何人可用公开数据独立验证全过程
```

在此架构下，人为信任被压缩到：**需 N 方中的 K 方串通才能提前解密；即使全部串通，泄露的也只是总数（反正要公开）；任何作弊都会被公开公告板上的零知识验证当场发现；机构集体缺席时，时间锁保证仍能出结果。**

| 信任模型 | 改造前（三家机构门限） | 改造后（去信任化） |
| --- | --- | --- |
| 信任基础 | 信任机构不作恶 | 作恶必定被发现 |
| 密钥泄露后果 | 全员投票内容暴露 | 仅提前泄露总数 |
| 完整密钥是否存在过 | 是（切割那一刻） | 否（DKG 保证） |
| 单票是否被解密 | 是 | 否（同态求和） |
| 解密时刻由谁决定 | 人为约定 | 数学（VDF 兜底） |
| 机构缺席会怎样 | 计票卡死 | 时间锁兜底出结果 |
| 能否追责 | 否 | 可指认参与分片 |

**结论：这不是"信任机构"，而是让机构处于"作恶代价极高、收益极低、且必定被发现"的结构中。**

---

## 5. 安全推演

### 5.1 胁迫场景

| 胁迫者手段 | 系统响应 | 结果 |
| --- | --- | --- |
| 站在身后监视投票 | 页面仅显示"成功"，不显示选项 | 无法确认投了什么 |
| 要求截图/录屏 | 截图无任何有效信息 | 无验收价值 |
| 要求"用真密码投 A" | 掩码输入，无法验证密码类型；投票者使用防胁迫密码 | 记录为弃权/忽略 |
| 逼迫反复投票 | 真实票已固化，后续票无法覆盖 | 胁迫无效 |
| 逼迫说出密码 | 投票者给出防胁迫密码；系统无验证接口，胁迫者无法确认真伪 | 真密码不泄露 |
| 自行使用该密码投票 | 系统识别为防胁迫密码 | 弃权/忽略 |
| 逼迫删除防胁迫密码 | 用户无自助删除权限，须本人到第三方机构 | 保护无法被解除 |
| 截止后逼迫投票 | 页面仍返回"成功" | 实际不记录 |
| 全程监视至截止 | 若真实票已提前投出则不受影响；否则为弃权 | 见 1.3 |
| 键盘记录器 | 真密码仅在可信设备输入，不可信设备上只出现防胁迫密码 | 录到的均为无效信息 |
| 破解随机数反推身份 | 随机数由 VPR 提供，锚定公共熵源，不可预测 | 无法反推代号→用户映射 |

### 5.2 收买场景

外部人员大规模撒钱买票时，由于系统**不提供任何可验证凭据**（不显示历史、不显示选项、截图无信息），买票者无法验收投票者是否履约。

**无法验收的交易无法规模化**——理性贿选者将放弃此类投资。

### 5.3 暴力逼问无法获得真密码（不属于不防御场景）

需要澄清一个关键结论：**暴力逼问并不构成这套方案的安全漏洞**。暴力与普通逼问，在"能否逼出真密码"这个维度上是完全等价的——两者都不具备验证手段。

若投票者被暴力逼问真密码，同样可给出**任意一个防胁迫密码**。由于：

- 系统不提供"验证某密码是否为真密码"的接口
- 两类密码的行为完全一致

胁迫者**没有预言机**，无论施加多大压力，都无法验证所得密码的真伪。真密码仍保留在投票者处。

因此，**"暴力逼问真密码"不列入第 1.3 节的"不防御威胁"清单**——它在本方案的技术覆盖范围内。唯一超出技术范畴的是暴力造成的人身伤害本身（属刑法救济范畴，非密码学问题）。

**使用纪律**：真密码仅在可信设备（个人手机、家用电脑）输入；在公司或任何不可信设备上，永远只输入防胁迫密码。

---

## 6. 技术规范

### 6.1 数据结构

**投票表（加密存储）**

| 字段 | 说明 |
| --- | --- |
| `ballot_token` | 真随机代号（VPR 派生，含盐） |
| `encrypted_ballot` | 客户端加密的选票密文（FHE） |
| `zk_proof` | 选票合法性零知识证明 |
| `submitted_at_bucket` | 时间戳（分桶，如精确到小时 + VPR 随机偏移） |

**注意**：数据库中**不设** `is_coerced` 字段。服务器不知道、也不需要知道某张票是否来自胁迫。

### 6.2 投票与结算逻辑

**投票接口（服务端）**

```python
def submit_vote(encrypted_ballot, zk_proof, session):
    # 验证选票合法但不解密（零知识证明）
    assert verify_zk(zk_proof, encrypted_ballot)
    store(session.ballot_token, encrypted_ballot, now_bucket())
    publish_to_bulletin_board(hash(encrypted_ballot))   # 写入公开公告板
    log(session.ballot_token)          # 仅记录代号，不记录用户 ID
    return {"message": "您的选票已成功记录。"}   # 任何情况下返回同一句话
```

**结算逻辑（同态求和，单票永不解密）**

```python
# 1. 同态求和：直接在密文上累加，无需解密任何单票
encrypted_tally = homomorphic_sum(all_encrypted_ballots)

# 2. 门限解密：只解密【总数】这一个值
tally = threshold_decrypt(encrypted_tally, shares)   # 需 K-of-N 分片

# 3. 输出并销毁
emit(tally)          # 仅输出各选项总数
destroy(shares, intermediate_data)
```

> 兜底路径：若截止后 T 天门限解密未完成，启动 VDF 时间锁，任何人可连续计算得到解密密钥。

### 6.3 日志规则

- 操作日志**不记录用户 ID**，只记录真随机会话代号
- 不记录使用了哪类密码，不记录输入内容
- 时间戳**分桶 + VPR 随机偏移**，切断与投票表的时间关联

> 若同时存在"用户 X 于 T 时投票"的日志与"(代号, T, 选票)"的记录，按时间戳 join 即可重建身份映射表，匿名性归零。这是最易被忽视的实现陷阱。

### 6.4 哈希审计链

每张选票与其真随机代号共同计算哈希，链式记录，创世盐由 VPR 派生（可选加后置锚定，见 3.6）：

```
H_0 = SHA-256( R(audit-chain) )
H_n = Hash( H_{n-1} ‖ ballot_token ‖ encrypted_ballot )
```

- 日常后台**不显示明细**
- 第三方机构可在结算前验证哈希链完整性，确认数据未被篡改
- 由于创世盐锚定公共熵源，第三方可进一步验证整条链**不是事后重算伪造的**

### 6.5 计票流程

1. 投票窗口关闭，进入 24 小时宽限期
2. **同态求和**：在密文上直接累加，得到各选项的加密总数（单票永不解密）
3. **门限解密**：K-of-N（建议 5-of-9）分片持有方注入分片，仅解密总数
4. 生成并公布计票过程的**零知识证明**，供任何人独立验证
5. 输出完成后立即销毁密钥分片与全部中间数据
6. 出具计票过程合规证明（不记载任何个体信息）
7. 第三方用公开熵源重放验证 VPR 随机性 → 验证审计链完整性
8. **兜底**：若步骤 3 在截止后 T 天（建议 30 天）内未完成，VDF 时间锁自动生效

**系统永不提供明细导出功能。** 不得实现任何形式的 `SELECT * FROM ballots` 查询接口。

### 6.6 异常监控与参与率门槛

**（1）最低参与率门槛——兜底"被迫沉默"**

- 设定最低有效参与率阈值（如 50%）：实际投票数 ÷ 合格人数低于阈值，本次结果无效，择期重投。
- 价值：胁迫者逼出大面积弃权、或全程隔离投票者致其无法补投真实票，都会把参与率打到阈值以下，使压制可被发现、选举可作废重投。这是技术上对"被迫沉默"唯一的兜底手段。
- 门槛与重投规则须写入选举章程，投票前公示锁定。

**（2）异常告警——把"无利可图"变成可审计指标**

- 持续监控参与率、弃权率等指标，建立历史基线。
- 当某项指标偏离历史基线超过 **2σ**（两倍标准差），自动触发第三方机构审查。
- 基线与阈值口径须在投票前公示并锁定，避免事后争议。

---

## 7. 实施前置条件

以下三条为**硬性前提**，缺一项则方案安全性不成立：

### 7.1 选举规则锁定（关键）

**必须明确规定：弃权票从分子与分母中同时剔除，禁止使用"全体成员过半数"等将弃权等价于反对的计票口径。**

理由：若采用"全体成员过半数同意"规则，弃权在效果上等同于反对。此时若胁迫者期望的正是"不通过"，则弃权会被动地帮助胁迫者达成目标。

该规则须写入选举章程，由外部机构背书，**任何一方不得事后解释或变更**。同时，6.6 节的最低参与率门槛与异常告警阈值也应一并写入章程锁定。

### 7.2 认证方式锁定

独立密码认证（禁用 SSO / 扫码 / 生物识别）须由外部契约固定，任何一方不得单方面变更。

### 7.3 服务器独立性保障

应用服务器、数据库、代码仓库须部署在胁迫者不可接触的环境，并由第三方机构监督。托管协议须明确权限边界与审计条款。

### 7.4 熵源清单锁定（新增）

VPR 的熵源清单、承诺哈希、组合算法须在投票开始前**公示并锁定**，任何一方不得事后变更熵源或组合规则——这是"可验证真随机数"可信的前提。

---

## 8. 实施流程

| 阶段 | 时间 | 任务 |
| --- | --- | --- |
| T-6 个月 | 设置窗口 | 全员完成真密码与防胁迫密码设置；模拟演练 |
| T-5 个月 | 机构签约与 DKG | 确定 N 家托管机构（建议 9 家，含对立利益相关方）；执行分布式密钥生成（完整私钥从未存在） |
| T-7 天 | 熵源承诺 | 公示 VPR 熵源清单与承诺哈希 |
| T-1 个月 | 规则公示 | 公布选举章程、计票口径、投票窗口 |
| T-1 天 | 熵源揭示 | 熵源事件发生，揭示并生成真随机数种子 R |
| T-0 | 投票开放 | 通过个人可信渠道强提醒：尽早用真密码投票 |
| T+30 天 | 窗口关闭 | 24 小时宽限期 |
| T+31 天 | 计票 | 同态求和 → 门限解密总数 → 公布 ZKP → 销毁中间数据 |
| T+31~T+61 天 | 兜底窗口 | 若门限未完成，VDF 时间锁自动生效 |
| T+32 天起 | 公开审计 | 第三方重放验证 VPR 随机性、审计链与计票 ZKP |
| 持续 | 运营 | 每季度推送安全提醒与操作纪律 |

---

## 9. 运营与用户纪律

### 9.1 尽早投票（核心运营动作）

投票开放即通过**个人可信渠道**（个人手机短信 / 个人邮箱，**非公司渠道**）推送提醒：

> "投票已开放。请在可信设备上使用真密码尽早投票——真实票一经投出即锁定，此后任何操作均无法覆盖。"

理由：真实票越早落地，胁迫者的可用窗口越小。

### 9.2 个人可信渠道核验

投票后，通过个人可信渠道反馈：

> "您已成功投出一票。当前真实票状态：已记录 / 未记录。"

这解决了"用户无法确认自己用了哪个密码"的可用性问题：

- 胁迫者不在投票者的个人设备上，**不会泄露**
- 投票者可在安全环境中确认真实票是否生效，若未生效仍可补投

### 9.3 用户纪律（须反复宣贯）

1. **真密码只在可信设备输入**；公司设备、公共设备上一律使用防胁迫密码
2. 被要求投票时，**永远使用防胁迫密码**——反正真实票已锁定，或结果无害
3. 被逼问密码时，给出任意一个防胁迫密码
4. 不可信设备上绝不输入真密码（防键盘记录器）

### 9.4 定期提醒

每季度推送一次安全提醒，内容包含上述纪律与当前密码状态（不含密码内容）。

---

## 10. 局限性

诚实声明本方案的边界：

| 局限 | 说明 | 缓解 |
| --- | --- | --- |
| **被迫沉默** | 胁迫者在投票期内全程隔离投票者，使其无法投出真实票，结果为弃权 | 最低参与率门槛（6.6）+ 制度与法律救济；参与率透明化使压制可被发现、可触发重投 |
| **选举被取消 / 结果不被承认** | 胁迫者宣布无效或无视结果 | 外部约束力与章程约束 |
| **自愿被收买** | 投票者自愿用真密码投出特定选项 | 系统不阻止，也不应阻止 |
| **暴力人身伤害** | 暴力本身超出技术防御范围（刑法范畴） | 技术无法阻止暴力；但暴力也无法逼出真密码，见 5.3 |
| **遗忘密码** | 长期不用可能混淆 | 设置后演练 + 季度提醒 + 机构远程救济通道 |
| **单方面变更规则** | 胁迫者修改认证方式或计票口径 | 由外部契约锁定，见第 7 节 |
| **熵源被操控** | 单一公共熵源被极强外力操控 | 多源组合稀释单一源风险；熵源清单提前公示锁定 |
| **密钥托管仍需人为安排** | 分片由谁持有，数学无法回答 | 五层加固 + 对立利益方制衡，见 4.5 |
| **VDF 算力竞赛** | 攻击者算力远超预期时可能提前解开时间锁 | T 值保守设置并留安全余量；仅作兜底，不作主路径 |

**本方案保护的是"那一票"，保护不了"这场选举"。** 它能防住"被迫支持"，防不住"被迫沉默"与"选举被取消"。跨越此边界需要的是制度设计（外部契约、参与率透明、申诉渠道），而非更多代码。

---

## 11. 核心心法

> **不要用"藏信息"防胁迫，要用"给假信息"。**

藏信息，胁迫者看不到，但他能盯着你操作；给假信息，胁迫者盯着你操作，也分不清真假。

> **让胁迫者榨取的净收益归零。**

防胁迫密码的产出是弃权，而非反对——胁迫者逼你投的那一票作废了，他什么也没得到。一个无利可图的胁迫，理性的人不会做第二次。

> **决胜点在于"安全地投出真实票的那一次"，而非"最后一次操作"。**

真实票落地即固化。因此运营的第一优先级是：**引导每一位投票者在可信环境中尽早投票。**

> **随机性不要交给系统自己，要交给不可控的公共世界。**

伪随机数是"系统拍脑袋"，可预测、不可审计；真随机数锚定公共熵源，不可预测、事后可验证——这是匿名性与可验证性共同的根基。

> **去信任化的目标不是消灭人，而是让作恶变得不划算。**

密码学无法回答"谁持有分片"，但它能做到：完整密钥从未存在、单票永不解密、作弊必定被发现、缺席也有数学兜底。最终形态不是"信任机构"，而是让机构处于"作恶代价极高、收益极低、且必定被发现"的结构中。

---

## 附：开发清单

**必须实现**

- [ ] 双密码认证，两类密码权限与反馈完全一致
- [ ] 客户端本地加密选票（FHE），服务端只存密文
- [ ] 选票合法性零知识证明 + 公开公告板
- [ ] 真密码优先结算逻辑
- [ ] 防胁迫密码可自助新增、不可自助删除
- [ ] 页面反馈永远只回一句话
- [ ] 日志去用户 ID，时间戳分桶 + VPR 随机偏移
- [ ] VPR 可验证真随机数（熵源承诺 + 揭示 + 组合验证）
- [ ] 随机代号、门限密钥分片、审计盐均由 VPR 派生
- [ ] 哈希审计链（创世盐锚定公共熵源，可选后置锚定）
- [ ] 同态求和计票（单票永不解密）
- [ ] DKG 分布式密钥生成（完整私钥从未存在）
- [ ] 门限解密（建议 5-of-9）仅解密总数
- [ ] VDF 时间锁兜底（截止后 T 天自动生效）
- [ ] 个人可信渠道核验推送
- [ ] 24 小时宽限期
- [ ] 熵源清单与承诺哈希提前公示锁定
- [ ] 最低有效参与率门槛判定（低于阈值 → 结果无效、择期重投）
- [ ] 参与率/弃权率异常监控（偏离基线 >2σ → 自动触发第三方审查）

**禁止实现**

- [ ] 倒计时 / 剩余天数显示
- [ ] 历史投票记录查询
- [ ] 任何形式的投票状态提示
- [ ] `SELECT * FROM ballots` 或任何明细导出
- [ ] `is_coerced` 之类的胁迫标记字段
- [ ] 密码类型验证接口（不得存在"验证某密码是否为真密码"的功能）
- [ ] SSO / 扫码 / 生物识别登录
- [ ] 软件伪随机数作为安全关键随机源（须替换为 VPR）
- [ ] 随机截止时间（采用统一公开投票窗口）
- [ ] 任何单张选票的解密功能（计票只解密总和）

---

*本白皮书为技术方案说明，部署前须经法务与合规审查，并依据适用法律法规及组织章程进行调整。同态加密、零知识证明、DKG、VDF 等模块建议委托具备应用密码学经验的团队实现并进行第三方安全审计。*
